# SESSION_SECRET dipakai untuk menandatangani cookie session (HS256).
# Salin berkas ini menjadi `.env.local` dan ganti nilainya, contoh:
#   openssl rand -base64 32
SESSION_SECRET=your_random_32_byte_secret